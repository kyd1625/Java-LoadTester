<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.repository.LoadTestFailLogRepository">

    <insert id="insertFailLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO load_test_fail_log (
                     result_id
                , 	request_order
                , 	error_msg
                , 	request_time
                , 	http_status
        ) VALUES(
                     #{resultId}
                , 	#{requestOrder}
                , 	#{errorMsg}
                , 	#{requestTime}
                , 	#{httpStatus}
        )
    </insert>

    <select id="selectFailLogByResultId" resultType="com.project.model.LoadTestFailLog">
        SELECT id
             , result_id AS resultId
             , request_order AS requestOrder
             , error_msg AS errorMsg
             , request_time AS requestTime
             , http_status AS httpStatus
        FROM load_test_fail_log
        WHERE result_id = #{resultId}
    </select>

    <delete id="deleteFailLogByResultId">
        DELETE FROM load_test_fail_log WHERE result_id = #{resultId}
    </delete>

</mapper>