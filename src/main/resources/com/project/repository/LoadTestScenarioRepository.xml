<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.repository.LoadTestScenarioRepository">

    <insert id="insertScenario" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO load_test_scenario (
                     name
                , 	target_url
                , 	http_method
                , 	request_params
                , 	target_tps
                , 	virtual_thread_count
                , 	duration_seconds
                , 	created_at
        )VALUES(
                    #{name}
               , 	#{targetUrl}
               , 	#{httpMethod}
               , 	#{requestParams}
               , 	#{targetTps}
               , 	#{virtualThreadCount}
               , 	#{durationSeconds}
               , CURRENT_TIMESTAMP
               )
    </insert>

    <select id="selectScenarios" resultType="com.project.model.LoadTestScenario">
        SELECT id
             , name AS name
             , target_url AS targetUrl
             , http_method AS httpMethod
             , request_params AS requestParams
             , target_tps AS targetTps
             , virtual_thread_count AS virtualThreadCount
             , duration_seconds AS durationSeconds
             , created_at AS createdAt
        FROM load_test_scenario
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY created_at DESC

    </select>

    <select id="selectScenarioById" resultType="com.project.model.LoadTestScenario">
        SELECT id
             , name AS name
             , target_url AS targetUrl
             , http_method AS httpMethod
             , request_params AS requestParams
             , target_tps AS targetTps
             , virtual_thread_count AS virtualThreadCount
             , duration_seconds AS durationSeconds
             , created_at AS createdAt
        FROM load_test_scenario
        WHERE id = #{id}
    </select>

    <delete id="deleteScenarioById">
        DELETE FROM load_test_scenario WHERE id = #{id}
    </delete>



</mapper>